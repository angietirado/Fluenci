# Copy to .env.local for local overrides.

# Local dev (default if unset in development):
REACT_APP_API_URL=http://localhost:5000

# Split deploy (frontend + API on different Vercel projects):
# REACT_APP_API_URL=https://fluenci-m5it.vercel.app

# Single Vercel project (same URL for UI + API): leave REACT_APP_API_URL unset in production
